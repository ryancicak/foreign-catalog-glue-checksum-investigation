# EMR cluster master node (public DNS)
EMR_HOST=ec2-xx-xx-xx-xx.us-west-2.compute.amazonaws.com
EMR_PEM_PATH=~/Downloads/your-key.pem

# Databricks workspace (OAuth2 service principal)
DATABRICKS_HOST=https://dbc-xxxxx.cloud.databricks.com
DATABRICKS_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
DATABRICKS_CLIENT_SECRET=dosexxxxxxxxxxxxxxxxx

# Table config (change these to match your setup, or leave defaults)
S3_BUCKET=your-glue-bucket-name
GLUE_DATABASE=your_glue_database
TABLE_NAME=hive_migrated_to_ice
FOREIGN_CATALOG=glue_catalog
AWS_REGION=us-west-2

# Only needed if you DON'T already have a foreign catalog set up in Databricks.
# This is a cross-account IAM role that Databricks UC can assume.
# Needs: S3 read on your bucket, Glue read access, and a trust policy
# allowing the Databricks UC AWS account (414351767826) to assume it.
# Leave blank if your foreign catalog already exists.
IAM_ROLE_ARN=
